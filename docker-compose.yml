services:

  analyst:

    build: .

    container_name: ai-data-analyst

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./plots:/app/plots

    restart: unless-stopped